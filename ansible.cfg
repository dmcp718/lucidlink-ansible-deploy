[defaults]
host_key_checking = False
remote_user = ubuntu

[ssh_connection]
ssh_args = -o ForwardAgent=yes

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
